import { EpyptianIDPipe } from './epyptian-id.pipe';

describe('EpyptianIDPipe', () => {
  it('create an instance', () => {
    const pipe = new EpyptianIDPipe();
    expect(pipe).toBeTruthy();
  });
});
